<div class="container">
  <div class="row my-4">
<h2 class="text-info text-opacity-25">検索一覧</h2>
<table class="table table-hover table-inverse">
      <thead>
        <tr>
          <th>投稿者</th>
          <th>タイトル</th>
          <th>投稿文</th>
          <th>コメント</th>
          <th>いいね</th>
          <th>投稿日時</th>
        </tr>
      </thead>
      <tbody>
        <% @posts.each do |post| %>
          <tr>
            <td>
              <%= link_to user_path(post.user.id) do %>
                <%= image_tag post.user.get_profile_image, class: "rounded-circle", size: "40x40"  %>
              <% end %>
            </td>
            <td>
              <%= link_to post_path(post.id) do %>
                <%= post.title %>
              <% end %>
            </td>
            <td><%= post.text %></td>
            <%#= post.place.name %>
            <td><%= link_to "#{post.comments.count} コメント", post_path(post.id) %></td>
            <td id='favorite_buttons_<%= post.id %>'>
              <%= render 'user/favorites/favorites', post: post%>
            </td>
            <td><%= post.created_at.strftime('%Y/%m/%d  %H:%M') %></td>
          </tr>
        <% end %>
      </tbody>
    </table>