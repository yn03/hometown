<%= form_with url: ranking_posts_path, method: :get, local: true do |f| %>
  <%= f.select :place_id, options_from_collection_for_select( Place.all, :id, :name), include_blank: "都道府県を選択" %>
  <%= f.submit "検索"%>
<%end%>

<% @posts.each do |post| %>
<%#= link_to user_path(post.user.id) do %>
<%= post.title %>
           <%= post.place.name %>

<% end %>