<hr>
<div class="container text-center py-4">
  <div class="row">
    <div class="card py-5 mx-auto bg-light" style="width: 27rem;">
      <h2>新規登録</h2>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "user/shared/error_messages", resource: resource %>

        <div class="field mt-3">
          <%= f.label :名前 %><br>
          <%= f.text_field :name, size: "30" %>
        </div>

        <div class="field mt-3">
          <%= f.label :メールアドレス %><br>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", size: "30" %>
        </div>

        <div class="field mt-3">
          <%= f.label :パスワード %>
          <% if @minimum_password_length %>
            <em>(<%= @minimum_password_length %>文字以上)</em>
          <% end %><br>
          <%= f.password_field :password, autocomplete: "new-password", size: "30"%>
        </div>

        <div class="field mt-3">
          <%= f.label :パスワード（確認用） %><br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password", size: "30" %>
        </div>

        <div class="actions mt-3">
          <%= f.submit "新規登録" %>
        </div>
      <% end %>

      <div class="mt-4">
        <h4>◎既に登録済みの方</h4>
      </div>
      <div class="mt-2"><u><%= link_to "→こちら", new_user_session_path %></u>からログインしてください。</div>

