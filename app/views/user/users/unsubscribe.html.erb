<div class="container">
  <div class="text-center mt-4">
    <h2><strong>本当に退会しますか？</strong></h2>
  </div>

  <div class="text-center mt-5 mb-5">
    <p>
      退会する場合は、「退会する」をクリックしてください。
    </p>
  </div>

  <div class="row justify-content-center">
    <div class="col-2"><%= link_to "退会しない", edit_user_path(current_user), class: "btn btn-primary" %></div>
    <div class="col-2"><%= link_to "退会する", withdraw_path, method: :patch, class: "btn btn-danger", "data-confirm" => "退会しますか？" %></div>
  </div>

</div>